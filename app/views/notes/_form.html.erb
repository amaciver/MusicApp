<form class="" action="<%= notes_url %>" method="post">
  <%= auth_token %>
  <textarea name="note[body]" rows="5" cols="20"></textarea>
  <input type="hidden" name="note[track_id]" value="<%= @track.id %>">

  <input type="submit" name="" value="Add Note">
</form>
